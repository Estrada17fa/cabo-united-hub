

## Plan: Rename "Zona de Partido" to "MATCH ZONE"

Change the nav link label in `src/components/layout/Header.tsx` from `"Zona de Partido"` to `"Match Zone"` (displayed as "MATCH ZONE" since `uppercase` is already applied).

### File: `src/components/layout/Header.tsx`
- Update the `navLinks` array entry: change `name: "Zona de Partido"` to `name: "Match Zone"`

